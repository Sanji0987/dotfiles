# personal scratch notes, not part of the config
*.local.md
local/

# backups
*.bak
*.bak.*
*.pre-dotfiles

# machine state, not config
config/fish/fish_variables

# everything below this line used to describe the omarchy theme pipeline: the
# walker stage-2 render, the kitty palette that was generated rather than
# written, the third-party theme clones, and a note explaining why waybar
# needed no entry. None of it generates anything now — every palette in this
# repo is a tracked, hand-maintained file — so there is nothing left to ignore.
