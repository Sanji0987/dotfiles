# --no-warn replaces unbindsym+bindsym pairs, simplified by Claude Code Opus
set $term kitty

bindsym --no-warn $mod+Return exec $term

set $rofi_cmd rofi -terminal '$term'
set $menu $rofi_cmd -show combi -combi-modes drun#run -modes combi
